server:
  port: 8080
  servlet:
    context-path: /api
spring:
  #redis ??
  redis:
    host: j8b207.p.ssafy.io
    port: 3301
    password: ns

  application:
    name: mything

  #??? ??
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  # ??? ?? ??
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://j8b207.p.ssafy.io:3300/mything?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false&allowPublicKeyRetrieval=true
    username: ns
    password: ns

  # JPA ??
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
#    show-sql: true
    hibernate:
      ddl-auto: create
      format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace


